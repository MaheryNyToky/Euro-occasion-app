services:
  postgres:
    image: postgres:16-alpine
    container_name: eurocasion-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE:-eurocasion}
      POSTGRES_USER: ${DB_USERNAME:-eurocasion}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
      TZ: UTC
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-eurocasion} -d ${DB_DATABASE:-eurocasion}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - eurocasion-network

  redis:
    image: redis:7-alpine
    container_name: eurocasion-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - eurocasion-network

  minio:
    image: quay.io/minio/minio:latest
    container_name: eurocasion-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-eurocasion_minio}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minio_secret_key}
    ports:
      - "${MINIO_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - eurocasion-network

  mailpit:
    image: axllent/mailpit:latest
    container_name: eurocasion-mailpit
    restart: unless-stopped
    ports:
      - "${MAILPIT_SMTP_PORT:-1025}:1025"
      - "${MAILPIT_UI_PORT:-8025}:8025"
    networks:
      - eurocasion-network

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  minio_data:
    driver: local

networks:
  eurocasion-network:
    driver: bridge
